// hooks/useFrameworkReady.ts
/**
 * This hook is used to signal to the Expo CLI that the app is ready to be used.
 * It's mainly for development purposes and doesn't affect production builds.
 */
export function useFrameworkReady() {
  // This hook is intentionally empty for now
  // In a real Expo app, this might be used for development-specific setup
  // but it's not required for the app to function
}